<h1>Listing budgets</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @budgets.each do |budget| %>
  <tr>
    <td><%= budget.name %></td>
    <td><%= budget.description %></td>
    <td><%= link_to 'Show', user_budget_path(budget) %></td>
    <td><%= link_to 'Edit', edit_user_budget_path(budget) %></td>
    <td><%= link_to 'Destroy', user_budget_path(budget), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Budget', new_user_budget_path(@user) %>
